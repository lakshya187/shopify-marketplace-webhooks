import logger from "#common-functions/logger/index.js";

export default async function OrderRefundedHandler(payload, metadata) {
  try {
    // const
  } catch (e) {
    logger("error", "[order-refunded-handler] Error in processing order", e);
  }
}
